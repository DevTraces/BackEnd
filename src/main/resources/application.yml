server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      force: true

spring:
  profiles:
    default: prod

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

